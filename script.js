const game = prompt(
  "Would you like to play a game with two character yes or no?"
);
const name = prompt("");
if (game === no) {
  console.log("Thank you for playing");
} else game;

if (damageAmount == 0) {
  damage = 0;
  playerHealth = playerHealth - damage;
  System.out.println(playerHealth);
} else if (damageAmount == 1) {
  damage = 1;
  playerHealth = playerHealth = damage;
  System.out.println(playerHealth);
} else if (damageAmount == 2) {
  damage = 2;
  playerHealth = playerHealth - damage;
  System.out.println(playerHealth);
}
if ((playerHealth = 0)) {
  gameover();
}
